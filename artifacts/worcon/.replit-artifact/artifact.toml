kind = "web"
previewPath = "/"
title = "Worcon AI 心理对话"
version = "1.0.0"
id = "artifacts/worcon"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22834

[services.development]
run = "pnpm --filter @workspace/worcon run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/worcon", "run", "build" ]
publicDir = "artifacts/worcon/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22834"
BASE_PATH = "/"
